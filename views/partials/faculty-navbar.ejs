</div>
<div class="main-panel">
  <nav class="navbar navbar-fixed-top" style="background-color:#EEEEEE; z-index:3">
      <div class="container-fluid">
          <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
          </div>
          <div class="collapse navbar-collapse">
              <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="/institution">
                      <i class="material-icons hidden-lg hidden-md">people</i>
                      <p>Institution</p>
                    </a>
                </li>
                  <li class="dropdown">
                     <a
                      <%if(authType == "principle" || authType == "admin"){%>
                        href="/admin"
                      <%}else{%>
                        href="/department?departmentId=<%=departmentId%>"
                      <%}%>
                        >                        
                        <i class="material-icons hidden-lg hidden-md">store</i>
                        <p>Department</p>
                      </a>
                  </li>
                  <li>
                      <a href="#pablo" class="dropdown-toggle" data-toggle="dropdown">
                          <i class="material-icons">person</i>
                          <p class="hidden-lg hidden-md">Profile</p>
                      </a>
                      <ul class="dropdown-menu">
                        <li>
                            <% if(authType == "hod" || authType == "coordinator") {%>
                                <a href="/department?departmentId=<%=departmentId%>">Profile</a>
                            <% } else if(authType == "principle" || authType == "admin"){ %>
                                <a href="/admin">Profile</a>
                            <%} else {%>
                                <a href="/faculty">Profile</a>
                            <%}%>    
                        </li>
                        <li>
                            <a href="/v1/apis/logout">Logout</a>
                        </li>
                      </ul>
                  </li>
                  <li>
                    <a style="background-color:#9C27B0; color:white;">
                     <% if(authType!="hod" && authType!="coordinator" && authType!="principle" && authType!="admin"){%>
                        Faculty
                     <%}else{%>
                        <%=authType%>
                     <%}%>
                    </a>
                  </li>
              </ul>
              <form class="navbar-form navbar-right" role="search">
                  <div class="form-group  is-empty">
                      <input type="text" class="form-control" placeholder="Search">
                      <span class="material-input"></span>
                  </div>
                  <button type="submit" class="btn btn-white btn-round btn-just-icon">
                      <i class="material-icons">search</i>
                      <div class="ripple-container"></div>
                  </button>
              </form>
          </div>
      </div>
  </nav>
  <div class="content">
